import { useEffect, useState } from 'react';
export default function Catalog() { const [meds,setMeds]=useState<any[]>([]); const [tests,setTests]=useState<any[]>([]); useEffect(()=>{fetch('http://localhost:4000/catalog/medicines').then(r=>r.json()).then(setMeds);fetch('http://localhost:4000/catalog/tests').then(r=>r.json()).then(setTests);},[]); return <div className='content'><h1>Catalog</h1><div className='card'><h3>Medicines</h3>{meds.map((m:any)=><div key={m._id}>{m.name}</div>)}</div><div className='card'><h3>Lab Tests</h3>{tests.map((t:any)=><div key={t._id}>{t.name}</div>)}</div></div>; }
